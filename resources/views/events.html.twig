<div class="row-fluid">
    <ul class="span3 offset1" id="listEvents">
        <li ng-repeat="(date,data) in events" class="item">
            <h1>{[date]}</h1>
            {[ data.description ]}
        </li>
    </ul>
</div>